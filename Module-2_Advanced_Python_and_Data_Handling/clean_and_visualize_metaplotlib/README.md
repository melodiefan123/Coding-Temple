#Data Cleaning Pipeline and Visualizations

Full data cleaning pipeline and meaningful visualizations. 

##Setup

1. Clone this repo
2. Create virtual environment: `python3 -m venv venv`
3. Activate: `source venv/bin/activate`
4. Install dependencies: `pip install -r requirements.txt`
5. Run: `python3 clean_and_visualize.py`

