# Module 2: Advanced Python & Data Handling — Starter Kit

## Quick Setup

### Option A: Clone with Git
```bash
git clone <repo-url>
cd module-02-advanced-python
```

### Option B: Download ZIP (no Git required)
1. Go to this repo on GitHub
2. Click the green **Code** button
3. Click **Download ZIP**
4. Unzip the downloaded file and open the folder

---

### Shared steps (both options)

1. **Create a virtual environment:**
   ```bash
   python -m venv venv
   source venv/bin/activate        # Mac/Linux
   venv\Scripts\activate           # Windows
   ```

2. **Install dependencies:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Start working:** Open any exercise folder and edit `starter.py`.

---

## Exercises

| # | Exercise | Folder | Packages Used |
|---|----------|--------|---------------|
| L1 | Big O Detective | `exercises/big-o-detective/` | None — built-in Python |
| L2 | Space Analysis | `exercises/space-analysis/` | None — built-in Python |
| L3 | Build and Use | `exercises/build-and-use/` | None — built-in Python |
| L4 | Library Catalog | `exercises/library-catalog/` | None — built-in Python |
| L5 | Functional Data Processor | `exercises/functional-data-processor/` | None — built-in Python |
| L6 | The Data Cleaner | `exercises/data-cleaner/` | None — `re` is built-in |
| L7 | Student Score Analysis | `exercises/student-score-analysis/` | **pandas** |
| L8 | Clean and Visualize | `exercises/clean-and-visualize/` | **pandas, matplotlib** |

> **Lessons 1–6** use only the Python standard library — no packages needed.
> **Lessons 7–8** and the module project require pandas and matplotlib.
> Make sure your virtual environment is active before running those exercises.

---

## Module Project

The project starter code is in `project/starter/`. See the project
instructions on the course platform for full requirements.

```bash
cd project/starter
python main.py
```

---

## Solutions

Solutions are in the `solutions/` folder. **Try each exercise yourself first!**
Compare your approach to the reference solution after you've made your attempt.
Differences are fine — there are many valid ways to solve these problems.

---

## Need Help?

- Re-read the lesson's Concept and Guided Example sections
- Post in the course discussion board
- Bring questions to office hours
